# Runtime dependencies. Prefer `pip install -e .` — this file exists for
# environments that cannot use pyproject.toml.
#
# Hardware extras (Raspberry Pi only):  pip install -e ".[hardware]"
# Development extras:                   pip install -e ".[dev]"

PyYAML>=6.0
Flask>=3.0
APScheduler>=3.10
